# Chainlit API surface security tests

Placeholder: add checks when Chainlit is deployed (e.g. IAM invoker, optional auth).
No tests yet; this directory reserves the namespace for future Chainlit-specific security tests.
