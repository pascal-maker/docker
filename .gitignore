__pycache__/
.claude/settings.local.json
.DS_Store
.env
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
apps/backend/.venv/
*.code-workspace
*.egg-info/
*.pyc
dist/
node_modules/
.nx/

# VS Code extension build output
apps/vscode-extension/out/

# Tool runtime state
registered_agents.json
task_agent_mapping.json

# Refactor-issues dashboard (local SQLite)
dashboard.db

.chainlit
chainlit.md
apps/backend/src/.files

# Cloned playgrounds (third-party)
playground/nestjs-layered-architecture/

# CI report
report.md

# Generated Cloud Function requirements (from make functions-export)
functions/*/requirements.txt

# TypeScript function deploy output (from make functions-build)
functions/auth_callback/.deploy/
functions/auth_register_device/.deploy/
functions/github_webhook/.deploy/

# Terraform (infra/). Commit .terraform.lock.hcl for consistent provider versions.
# Run terraform from infra/ (GCS backend); never from infra/site/ (that creates local state).
infra/.terraform/
infra/site/.build/
infra/*.tfstate
infra/*.tfstate.*
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/*.tfvars
infra/dev.tfvars
infra/secrets.tfvars
gh-actions-key.json
.refactor-agent-a2a-url

firebase-sa.json

# GitHub App private key (keep in infra/, reference via file() in secrets.tfvars)
infra/*.pem
