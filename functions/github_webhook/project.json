{
  "name": "fn-github-webhook",
  "root": "functions/github_webhook",
  "projectType": "application",
  "tags": ["scope:functions", "type:app", "lang:python"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "{projectRoot}/**/*.py",
        "{workspaceRoot}/pyproject.toml"
      ],
      "options": {
        "command": "uv run --package refactor-agent ruff check functions/github_webhook/",
        "cwd": "{workspaceRoot}"
      }
    },
    "bundle": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv export --frozen --no-dev --no-editable --no-emit-project --package github-webhook -o functions/github_webhook/requirements.txt",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
