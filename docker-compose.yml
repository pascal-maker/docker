# A2A server (sync + A2A in one container) and LiteLLM proxy.
# Set ANTHROPIC_API_KEY (env or .env). Optional: LANGFUSE_* for prompt registry.
# To run only the A2A server: docker compose up a2a-server
#
# Open WebUI stack: adapter + Open WebUI. One command: docker compose up adapter openwebui
# (or docker compose up -d adapter openwebui). Open WebUI at http://localhost:3000;
# adapter at http://localhost:8000. Connection is set via env (no Admin UI).
services:
  litellm:
    image: docker.litellm.ai/berriai/litellm:main-latest
    ports:
      - "4000:4000"
    volumes:
      - ./config/litellm.yaml:/app/config.yaml
    environment:
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    command: ["--config", "/app/config.yaml"]

  a2a-server:
    build: .
    ports:
      - "8765:8765"   # WebSocket sync
      - "9999:9999"   # A2A HTTP
    environment:
      REPLICA_DIR: /workspace
      POC_SYNC_PORT: "8765"
      LITELLM_PROXY_URL: http://litellm:4000
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_BASE_URL: ${LANGFUSE_BASE_URL:-}
    depends_on:
      - litellm
    # Replica is in-container only; sync client pushes code via WebSocket
